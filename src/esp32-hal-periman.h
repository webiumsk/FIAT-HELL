#pragma once
// Stub: GFX Library includes this header but uses none of its symbols.
// PlatformIO's arduino-esp32 package does not ship esp32-hal-periman.h,
// so we provide an empty placeholder to satisfy the include.
